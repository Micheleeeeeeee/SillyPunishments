package me.sillysock.sillypunishments.sillyapi;

public enum PunishmentType {

  BAN(), // EXCLUSION
  KICK(), // Kick player from server
  MUTE(), // Mute player [unable to speak]
  OTHER(); // Other punishment [probably only used if error]

}
